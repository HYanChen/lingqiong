# 图形工作流模块下线说明

自 2026-07-12 起，官网和项目中心不再提供 ComfyUI 画布、代理入口、安装命令或生产流程卡片。

项目生产统一收敛到 `/projects`：

- 剧集与剧本管理
- 角色、场景、道具资产
- 分镜图片与视频
- 配音
- 分镜合成
- 灵穹 API 生成任务与调用记录

旧 `/canvas` 仅保留兼容跳转并返回项目中心；`/comfyui/*` 网关代理已移除。历史项目画布数据暂不物理删除，避免升级时误删用户资料，但不再作为用户可访问功能。
